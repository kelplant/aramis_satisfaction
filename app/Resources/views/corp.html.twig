{% block mainform %}
<div class="container">
    {{ form_start(satisfactionForm, {'attr':{'class': 'form-horizontal'}}) }}
    {{ form_errors(satisfactionForm) }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class=" col-sm-8 corp-message-2">
            <div class="form-group alert-info corp-message-1">
                Bonjour, Merci à vous d'avoir prit le temps de venir jusqu'ici nous faire un retour sur le traitement de votre ticket.<br>
                Dans le cas ou votre retour ne serait pas positif merci de nous laisser un commentaire afin que nous puissions nous améliorer.
            </div>
        </div>
    </div>
    {% if message is defined %}
        <div class="form-group">
            <div class="col-sm-2"></div>
            <div class=" col-sm-8 corp-message-3">
                <div class="form-group alert-success corp-message-1">
                {{ message }}
                </div>
            </div>
        </div>
    {% endif %}
    <div class="form-group">
        {{ form_label(satisfactionForm.NumTicket) }}
        {{ form_errors(satisfactionForm.NumTicket) }}
        <div class="col-sm-8">
            {{ form_widget(satisfactionForm.NumTicket) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(satisfactionForm.Sujet) }}
        {{ form_errors(satisfactionForm.Sujet) }}
        <div class="col-sm-8">
            {{ form_widget(satisfactionForm.Sujet) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(satisfactionForm.Description) }}
        {{ form_errors(satisfactionForm.Description) }}
        <div class="col-sm-8">
            {{ form_widget(satisfactionForm.Description) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(satisfactionForm.Satisfaction) }}
        {{ form_errors(satisfactionForm.Satisfaction) }}
        <div class="col-sm-8">
            <i class="fa fa-thumbs-o-down redIconColor"></i>
            {% for choix in satisfactionForm.Satisfaction %}
                <td>
                    {{ form_widget(choix) }}
                    {{ form_label(choix) }}
                </td>
            {% endfor %}
            <i class="fa fa-thumbs-o-up greenIconColor"></i>
        </div>
    </div>
    <div class="form-group">
        {{ form_label(satisfactionForm.Conformite) }}
        {{ form_errors(satisfactionForm.Conformite) }}
        <div class="col-sm-8">
            <i class="fa fa-thumbs-o-down redIconColor"></i>
            {% for choix in satisfactionForm.Conformite %}
                <td>
                    {{ form_widget(choix) }}
                    {{ form_label(choix) }}
                </td>
            {% endfor %}
            <i class="fa fa-thumbs-o-up greenIconColor"></i>
        </div>
    </div>
    <div class="form-group">
        {{ form_label(satisfactionForm.Accompagnement) }}
        {{ form_errors(satisfactionForm.Accompagnement) }}
        <div class="col-sm-8">
            <i class="fa fa-thumbs-o-down redIconColor"></i>
            {% for choix in satisfactionForm.Accompagnement %}
                <td>
                    {{ form_widget(choix) }}
                    {{ form_label(choix) }}
                </td>
            {% endfor %}
            <i class="fa fa-thumbs-o-up greenIconColor"></i>
        </div>
    </div>
    <div class="form-group">
        {{ form_label(satisfactionForm.Delais) }}
        {{ form_errors(satisfactionForm.Delais) }}
        <div class="col-sm-8">
            <i class="fa fa-thumbs-o-down redIconColor"></i>
            {% for choix in satisfactionForm.Delais %}
                <td>
                    {{ form_widget(choix) }}
                    {{ form_label(choix) }}
                </td>
            {% endfor %}
            <i class="fa fa-thumbs-o-up greenIconColor"></i>
        </div>
    </div>
    <div class="form-group">
        {{ form_label(satisfactionForm.Commentaires) }}
        {{ form_errors(satisfactionForm.Commentaires) }}
        <div class="col-sm-8">
            {{ form_widget(satisfactionForm.Commentaires) }}
        </div>
    </div>

    {{ form_row(satisfactionForm._token) }}
    <div style="display: none">
        {{ form_row(satisfactionForm.numticket) }}
    </div>

    <div style="display: none">
        {{ form_row(satisfactionForm.id) }}
    </div>
{% endblock %}