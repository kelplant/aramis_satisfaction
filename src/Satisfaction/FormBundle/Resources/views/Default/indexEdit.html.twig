{% extends '::base.html.twig' %}
{% block title %} Formulaire Non Répondu {% endblock %}
{% block mainform %}{% include '::corp.html.twig' %}{% endblock %}
{% block footform %}
            <div class="form-group">
                <div class="col-sm-2"></div>
                <div class="col-sm-4">
                    {{ form_row(satisfactionForm.Envoyer) }}
                </div>
            </div>
        {{ form_end(satisfactionForm) }}
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/jquery.dataTables.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('js/dataTables.bootstrap.js') }}" type="text/javascript"></script>
    <script>

        $("input[name~='ticket[Satisfaction]']").bind('change', function() {
            var satisfactionValue = parseInt($(this).val());
            if (satisfactionValue < 5) {
                $("#ticket_Commentaires").attr('required', 'required');
            } else {
                $("#ticket_Commentaires").removeAttr('required');
            }
        });
    </script>

{% endblock %}